[actual] # this will be the tar name and also is the directory name
server files/account.sqlite
user files/**/*.sqlite
user files/**/metadata.json

[recyclarr]
recyclarr.{yaml,yml}

[duplicati]
.config/
control_dir_v2/
*.sqlite
